mongo:
  image: mongo:jessie
  volumes:
    - ./.dbdata:/data/db

redis:
  image: redis:3.2-alpine
  volumes:
    - ./.redis:/data/db

si32-api:
  build: .
  ports:
    - "3000:3000"
  volumes:
    - .:/app
  links:
    - mongo
    - redis
